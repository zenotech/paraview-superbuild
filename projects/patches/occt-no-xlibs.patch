diff -u -Nru5 occt-7.4.0p1/adm/cmake/occt_csf.cmake src/adm/cmake/occt_csf.cmake
--- occt-7.4.0p1/adm/cmake/occt_csf.cmake	2020-04-01 16:52:54.000000000 -0400
+++ src/adm/cmake/occt_csf.cmake	2020-08-03 10:30:04.116130064 -0400
@@ -96,11 +96,11 @@
       find_library (OpenGlLibs_LIB NAMES OpenGLES)
       set (CSF_OpenGlLibs ${OpenGlLibs_LIB})
       OCCT_CHECK_AND_UNSET (OpenGlLibs_LIB)
     elseif (USE_GLX)
       set (CSF_OpenGlLibs GL)
-      set (CSF_XwLibs     "X11 Xext Xmu Xi")
+      set (CSF_XwLibs     "X11 Xext")
     else()
       find_library (OpenGlLibs_LIB NAMES OpenGL)
       set (CSF_OpenGlLibs ${OpenGlLibs_LIB})
       OCCT_CHECK_AND_UNSET (OpenGlLibs_LIB)
     endif()
@@ -113,10 +113,10 @@
     if (USE_GLES2)
       set (CSF_OpenGlLibs  "EGL GLESv2")
     else()
       set (CSF_OpenGlLibs  "GL")
     endif()
-    set (CSF_XwLibs      "X11 Xext Xmu Xi")
+    set (CSF_XwLibs      "X11 Xext")
     set (CSF_dl          "dl")
     set (CSF_fontconfig  "fontconfig")
   endif()
 endif()
diff -u -Nru5 occt-7.4.0p1/adm/cmake/occt_toolkit.cmake src/adm/cmake/occt_toolkit.cmake
--- occt-7.4.0p1/adm/cmake/occt_toolkit.cmake	2020-04-01 16:52:54.000000000 -0400
+++ src/adm/cmake/occt_toolkit.cmake	2020-08-03 10:30:04.103130333 -0400
@@ -319,11 +319,11 @@
 endforeach()
 
 if (APPLE)
   list (FIND USED_EXTERNAL_LIBS_BY_CURRENT_PROJECT X11 IS_X11_FOUND)
   if (NOT ${IS_X11_FOUND} EQUAL -1)
-    find_package (X11 COMPONENTS X11 Xext Xmu Xi)
+    find_package (X11 COMPONENTS X11 Xext)
     if (NOT X11_FOUND)
       message (STATUS "Warning: X11 is not found. It's required to install The XQuartz project: http://www.xquartz.org")
     endif()
   endif()
 endif()
diff -u -Nru5 occt-7.4.0p1/adm/genconfdeps.tcl src/adm/genconfdeps.tcl
--- occt-7.4.0p1/adm/genconfdeps.tcl	2020-04-01 16:52:54.000000000 -0400
+++ src/adm/genconfdeps.tcl	2020-08-03 10:30:04.090130602 -0400
@@ -1141,17 +1141,17 @@
   set isFound "true"
   if { "$::tcl_platform(platform)" == "windows" || ( "$::tcl_platform(os)" == "Darwin" && "$::MACOSX_USE_GLX" != "true" ) } {
     return "$isFound"
   }
 
-  set aXmuLibPath [wokdep:SearchLib "Xmu" "$::ARCH"]
-  if { "$aXmuLibPath" == "" } {
-    set aXmuLibPath [wokdep:SearchLib "Xmu" "$::ARCH" "/usr/X11/lib"]
-    if { "$aXmuLibPath" != "" } {
+  set aXextLibPath [wokdep:SearchLib "Xext" "$::ARCH"]
+  if { "$aXextLibPath" == "" } {
+    set aXextLibPath [wokdep:SearchLib "Xext" "$::ARCH" "/usr/X11/lib"]
+    if { "$aXextLibPath" != "" } {
       #lappend ::CSF_OPT_LIB$::ARCH "/usr/X11/lib"
     } else {
-      lappend anErrLib$::ARCH "Error: '${::SYS_LIB_PREFIX}Xmu.${::SYS_LIB_SUFFIX}' not found (X11)"
+      lappend anErrLib$::ARCH "Error: '${::SYS_LIB_PREFIX}Xext.${::SYS_LIB_SUFFIX}' not found (X11)"
       set isFound "false"
     }
   }
 
   return "$isFound"
diff -u -Nru5 occt-7.4.0p1/adm/genproj.tcl src/adm/genproj.tcl
--- occt-7.4.0p1/adm/genproj.tcl	2020-04-01 16:52:54.000000000 -0400
+++ src/adm/genproj.tcl	2020-08-03 10:30:21.780764971 -0400
@@ -1426,11 +1426,11 @@
         set aLibsMap(CSF_OpenGlLibs) "EGL GLESv2"
       } else {
         set aLibsMap(CSF_ThreadLibs) "pthread rt"
         set aLibsMap(CSF_OpenGlLibs) "GL"
         set aLibsMap(CSF_TclTkLibs)  "X11 tk8.6"
-        set aLibsMap(CSF_XwLibs)     "X11 Xext Xmu Xi"
+        set aLibsMap(CSF_XwLibs)     "X11 Xext"
         set aLibsMap(CSF_MotifLibs)  "X11"
       }
 
       if { "$::HAVE_GLES2" == "true" } {
         set aLibsMap(CSF_OpenGlLibs) "EGL GLESv2"
diff -u -Nru5 occt-7.4.0p1/adm/qmake/OccToolkit.pri src/adm/qmake/OccToolkit.pri
--- occt-7.4.0p1/adm/qmake/OccToolkit.pri	2020-04-01 16:52:54.000000000 -0400
+++ src/adm/qmake/OccToolkit.pri	2020-08-03 10:30:04.123129919 -0400
@@ -59,11 +59,11 @@
 } else {
   CSF_dl = -ldl
   CSF_ThreadLibs = -lpthread -lrt
   CSF_OpenGlLibs = -lGL
   CSF_TclTkLibs  = -lX11 -ltk8.6
-  CSF_XwLibs     = -lX11 -lXext -lXmu -lXi
+  CSF_XwLibs     = -lX11 -lXext
   CSF_MotifLibs  = -lX11
   CSF_fontconfig = -lfontconfig
   HAVE_GLES2 { CSF_OpenGlLibs = -lEGL -lGLESv2 }
 }
 
